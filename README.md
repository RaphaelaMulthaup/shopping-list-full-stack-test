# shopping-list-full-stack-test
A simple full stack application (React + TypeScript + Express + MongoDB / Mongoose) that allows users to add, check off, and delete products from their shopping list.

## 🎨 UI Library
This project utilizes the **Material UI (MUI)** library. It is based on **Material Design** principles, which allows for the rapid development of a modern, responsive, and accessible user interface.